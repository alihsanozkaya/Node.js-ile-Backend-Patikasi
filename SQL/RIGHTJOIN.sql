-- SELECT first_name, last_name, title FROM book
-- RIGHT JOIN author ON book.author_id = author.id;

-- SELECT first_name, last_name, title FROM book
-- RIGHT OUTER JOIN author ON book.author_id = author.id;